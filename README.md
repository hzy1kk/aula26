# Atividade Flask - 3 páginas

Aplicação simples com Flask e 3 rotas HTML.

## Como rodar

```bash
pip install -r requirements.txt
python app.py
```

Abra no navegador: http://127.0.0.1:5000

## Páginas

- `/` - Início
- `/sobre` - Sobre
- `/contato` - Contato
